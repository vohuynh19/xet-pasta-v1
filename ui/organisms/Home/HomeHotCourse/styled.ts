import styled from "styled-components";
import { container } from "styles";

export const Container = styled.section`
  ${container}
  background: ${`linear-gradient(180deg, white 0%, white 90% ,#EAF1F880 100%)`};

  padding-bottom: 80px;
`;
